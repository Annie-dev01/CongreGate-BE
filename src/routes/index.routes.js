const userRoutes = require('./users.routes');
const testimonyRoutes = require('./testimonies.routes');
const announcementRoutes = require('./announcement.route');

module.exports = Object.freeze({
  userRoutes,
  testimonyRoutes,
  announcementRoutes
});
